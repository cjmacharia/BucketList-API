version: 2
jobs:
  build:
    docker:
      - image: circleci/python
      - image: postgres:9.6
    steps:
      - checkout
      - run:
          name: install python requirements
          command: pip install requirements.txt
